1). Refactor code.
2). Randomizer of effects.
3). Make effect follow the cursor, so you experience how difficult it is for vision disabilities.
4). Button that removes all aria-label labels + button to let the page start reading content.
    - randomly remove some aria-labels so the story doesnt make sence
    - Remove all the aria-labels so the user cannot listen to anything.








BUGFIX
1). effect wond load after page switch.
